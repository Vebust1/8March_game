server {
    listen 80;
    server_name твой-домен.ru;

    # После получения сертификата раскомментируй блок ниже и закомментируй location выше
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://app:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }
}

# Раскомментируй после получения SSL-сертификата:
# server {
#     listen 443 ssl;
#     server_name твой-домен.ru;
#
#     ssl_certificate /etc/letsencrypt/live/твой-домен.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/твой-домен.ru/privkey.pem;
#
#     location / {
#         proxy_pass http://app:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
